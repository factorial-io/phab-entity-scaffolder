label: Novas Ark
id: novas_ark
description: A block_content to test all fields.
fields:
  # checkbox
  # cta
  cta:
    map: cta
    type: cta
    label: CTA
    required: true
  # image
  image:
    map: img
    type: image
    label: Image
    required: true
  # link
  # radio
  # select
  # text
  label:
    map: label
    type: text
    label: Label
    required: true
  # textarea
  body:
    map: body
    type: textarea
    label: Body
    required: true
  # reference_node
  content:
    map: content
    type: reference_node
    label: Content
    required: false
    target:
      - basic_page
#      - articles
